# Task: Implement Assigned Task

Implement the assigned coding task in this repository.
Inspect the relevant code, write or modify files, run the test suites, and ensure all tests pass.

## CRITICAL: Creating Code & Starting State
- **Code is to be created from scratch.**
- A missing file, an empty file, or a lack of existing implementation is **NEVER a blocker**—it is simply the normal starting state for building new functionality!
- If a file is empty or does not exist, use `write_file` to create it with the full implementation.
- If directory structures are needed, create them.
- If dependencies are needed, install them.

## Workflow:
1. Review the task description and requirements carefully.
2. Inspect the current codebase to understand patterns, styles, and existing test setups.
3. If files do not exist yet or are empty, create and write them! Implement the feature with clean, maintainable code.
4. Add or update unit tests to verify your implementation.
5. Run the project tests and linters (`pnpm test`, `npm test`, `pnpm run typecheck`, etc.).
6. If any tests fail, investigate and fix until they pass.

## If Complete:
Summarize the changes and validation steps, then conclude with:
```
<status>done</status>
```

## If Blocked:
ONLY output `<status>blocked</status>` if you encounter a genuine, insurmountable external impediment that CANNOT be solved by writing code or installing packages (e.g. missing external third-party API credentials, corrupted hardware, or fundamentally contradictory requirements).
NEVER output `<status>blocked</status>` because code needs to be written or files are empty.
If genuinely blocked:
```
<blocked-summary>Summary of blocker</blocked-summary>
<blocked-impact>Impact on progress</blocked-impact>
<blocked-next-step>Recommended step to unblock</blocked-next-step>
<blocked-needs>Dependency or input needed</blocked-needs>
<status>blocked</status>
```
